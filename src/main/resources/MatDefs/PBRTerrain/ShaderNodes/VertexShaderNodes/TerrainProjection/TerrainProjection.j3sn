ShaderNodeDefinitions {
     ShaderNodeDefinition TerrainProjection {
        Type: Vertex
        Shader GLSL100: MatDefs/PBRTerrain/ShaderNodes/VertexShaderNodes/TerrainProjection/TerrainProjection.vert

        Documentation {
            Terrain Vertex shader node
            @input worldViewProjectionMatrix World View Projection Matrix
            @input inPosition Input Position

            @output vVertex Vertex position
        }

        Input {
            mat4 worldViewProjectionMatrix;
            vec3 inPosition;
            vec3 inNormal;
        }

        Output {
            vec3 vPosition;
            vec3 vNormal;

            vec4 projPosition;
        }
    }
} 
